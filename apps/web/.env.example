# apps/web env — mirrors root .env.example for convenience.
# Local dev: copy to .env.local (gitignored).
# Staging/prod: set in Vercel dashboard.

SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
